# 逍遥游

个人 AI 产品实操博客。

## 发布文章

1. 在 `content/posts/` 下创建文章目录
2. 写 `index.md`，截图放 `images/` 文件夹
3. 执行 `git add . && git commit -m "发布：文章标题" && git push`

## 本地预览

```bash
hugo server -D
```

打开 http://localhost:1313 预览。
